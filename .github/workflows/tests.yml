name: Tests (Vitest + Cypress)

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  backend-unit-tests:
    name: Backend Unit Tests (Vitest)
    runs-on: ubuntu-latest
    
<<<<<<< Updated upstream
=======
    strategy:
      matrix:
        node-version: [20.x, 22.x]
    
>>>>>>> Stashed changes
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci
      
      - name: Generate Prisma Client
        working-directory: ./backend
<<<<<<< Updated upstream
        run: npm test 2>&1 || true
        continue-on-error: true
      
      - name: Check if coverage exists
        working-directory: ./backend
        run: ls -la coverage/ 2>&1 || echo "No coverage directory"
=======
        run: npx prisma generate
      
      - name: Run backend unit tests only
        working-directory: ./backend
        env:
          JWT_SECRET: test_secret_key
        run: npm run test:unit -- --run
      
      - name: Upload backend coverage
        if: matrix.node-version == '22.x'
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/coverage/coverage-final.json
          flags: backend-unit
          token: ${{ secrets.CODECOV_TOKEN }}
        continue-on-error: true
>>>>>>> Stashed changes

  backend-integration-tests:
    name: Backend Integration Tests (Vitest)
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: mysql
          MYSQL_DATABASE: s13_spl_test
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
        ports:
          - 3306:3306
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci
      
      - name: Wait for MySQL
        run: |
<<<<<<< Updated upstream
          for i in {1..30}; do
            if mysqladmin ping -h"127.0.0.1" -u"root" -p"mysql" --silent 2>/dev/null; then
              echo "MySQL is ready"
              exit 0
            fi
            echo "Waiting for MySQL ($i/30)..."
            sleep 1
          done
          echo "MySQL failed to start"
          exit 1
=======
          until mysqladmin ping -h"127.0.0.1" -u"root" -p"mysql" --silent; do
            echo 'Waiting for MySQL...'
            sleep 2
          done
          echo "MySQL is ready!"
      
      - name: Setup Database
        working-directory: ./backend
        env:
          DATABASE_URL: mysql://root:mysql@127.0.0.1:3306/s13_spl_test
        run: |
          npx prisma generate
          npx prisma db push --accept-data-loss
>>>>>>> Stashed changes
      
      - name: Run backend integration tests
        working-directory: ./backend
        env:
          DATABASE_URL: mysql://root:mysql@127.0.0.1:3306/s13_spl_test
<<<<<<< Updated upstream
          JWT_SECRET: test_secret
        run: npm test 2>&1 || true
=======
          JWT_SECRET: test_secret_key
        run: npm run test:integration -- --run
      
      - name: Upload integration coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/coverage/coverage-final.json
          flags: backend-integration
          token: ${{ secrets.CODECOV_TOKEN }}
>>>>>>> Stashed changes
        continue-on-error: true

  frontend-unit-tests:
    name: Frontend Unit Tests (Vitest)
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: SPL/package-lock.json
      
      - name: Install frontend dependencies
        working-directory: ./SPL
        run: npm ci
      
      - name: Run frontend unit tests
        working-directory: ./SPL
<<<<<<< Updated upstream
        run: npm test
        continue-on-error: true
      
      - name: Upload frontend coverage (if exists)
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: frontend-coverage
          path: ./SPL/coverage
          if-no-files-found: ignore
=======
        run: npm test -- --run
      
      - name: Upload frontend coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./SPL/coverage/coverage-final.json
          flags: frontend-unit
          token: ${{ secrets.CODECOV_TOKEN }}
        continue-on-error: true
>>>>>>> Stashed changes

  e2e-tests:
    name: E2E Tests (Cypress)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
      
<<<<<<< Updated upstream
      - name: Install dependencies
        run: |
          cd backend && npm ci && cd ..
          cd SPL && npm ci && cd ..
      
      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        with:
          working-directory: .
          browser: chrome
          headless: true
          spec: cypress/e2e/**/*.cy.js
          record: false
=======
      - name: Install all dependencies (root)
        run: npm install
      
      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci
      
      - name: Install frontend dependencies
        working-directory: ./SPL
        run: npm ci
      
      - name: Wait for MySQL
        run: |
          until mysqladmin ping -h"127.0.0.1" -u"root" -p"mysql" --silent; do
            echo 'Waiting for MySQL...'
            sleep 2
          done
          echo "MySQL is ready!"
      
      - name: Setup Database
        working-directory: ./backend
        env:
          DATABASE_URL: mysql://root:mysql@127.0.0.1:3306/s13_spl_local
        run: |
          npx prisma generate
          npx prisma db push --accept-data-loss
      
      - name: Start backend server
        working-directory: ./backend
        env:
          DATABASE_URL: mysql://root:mysql@127.0.0.1:3306/s13_spl_local
          JWT_SECRET: test_secret_key
          NODE_ENV: test
          PORT: 4000
        run: npm start &
      
      - name: Start frontend dev server
        working-directory: ./SPL
        env:
          VITE_API_URL: http://localhost:4000
        run: npm run dev &
      
      - name: Wait for servers to start
        run: |
          echo "Waiting for frontend..."
          npx wait-on http://localhost:5173 --timeout 60000
          echo "Servers are ready!"
      
      - name: Run Cypress tests
        run: npm run cy:run
>>>>>>> Stashed changes
        env:
          CYPRESS_baseUrl: http://localhost:5173
        continue-on-error: true
      
      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore
      
      - name: Upload Cypress videos
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: cypress/videos
          if-no-files-found: ignore

  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [backend-unit-tests, backend-integration-tests, frontend-unit-tests, e2e-tests]
    if: always()
    
    steps:
      - name: Check test results
        run: |
          echo "## 📊 Test Results Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test Suite | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Backend Unit Tests | ${{ needs.backend-unit-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Backend Integration Tests | ${{ needs.backend-integration-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend Unit Tests | ${{ needs.frontend-unit-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| E2E Tests (Cypress) | ${{ needs.e2e-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          
      - name: Fail if any test failed
        if: |
          needs.backend-unit-tests.result == 'failure' ||
          needs.backend-integration-tests.result == 'failure' ||
          needs.frontend-unit-tests.result == 'failure' ||
          needs.e2e-tests.result == 'failure'
        run: exit 1